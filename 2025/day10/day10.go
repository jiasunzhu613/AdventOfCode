package main

import "math"

func main() {

}

func GetLeastButtonPresses(target, current, clicks int, buttons []int) int {
	if target == current {
		return clicks
	}

	result := math.Inf(1)
	for _, button := range buttons {
		result = 
	}
}